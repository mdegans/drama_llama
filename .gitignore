/target
models/*.gguf
/Cargo.lock
.claude/scheduled_tasks.lock
model.sampling.toml
/profile.py
/bench.py
/prefill_prompt.txt
/__pycache__/
/fuzz-corpus/
/prefill_prompt_long.txt
screenshots/
.claude/docs/*.pdf
.DS_Store
# Auto-generated per-model sampling sidecars (crate defaults written
# on first Session load; see future_work_gguf_sampling_defaults.md).
models/*.sampling.toml
# Local per-model deploy config: dialect overrides and template
# patches. The canonical patched templates live versioned under
# tests/fixtures/templates/ (the Gemma e2e suite installs its own
# copy here).
models/*.dialect.toml
models/*.template.jinja
