# Handoff: 2026-09-25 (end of the #113 arc and the first live Agora run)

Read first next session. Everything below is committed and pushed to `dev`.

## Landed (#113 arc and follow-ups)
- Multi-word known ids; `IgnoreCategory::Numbers` (default on); region
  guard protects multi-token exits (the tool-call run-ons); effort →
  `reasoning_effort` per template; `Thinking::Disabled` renders off;
  effort-only `output_config` requests no grammar (it caused the 09-23
  outage); `with_render_opts` keeps reingest; test.py `--tier` wins over
  `--filter`.
- Sidecars are versioned (`models/*.sampling.toml`, except
  `model.sampling.toml`). All served models carry the #113 block
  (Numbers + id_patterns). Locally typical p = 0.9, except gpt-oss at
  0.5 plus the Harmony header tokens in `ignored` (the root cause of
  #101).
- Examples: `id_fidelity`, `longform`, `replay` (with `--only`,
  `--model`).

## Open, prioritized
1. **#119**: blallama 400 on context overflow + `count_tokens`
   (balerion's request; spec in the issue).
2. **Cogito stalls replay**: the dumps are in the session scratchpad
   `stalls/` (they contain SOUL/memory text, so they stay out of git; ask
   Mike to scp `claude-agora@balerion:stall-samples-2026-09-24.tar.gz`
   again if the scratchpad is gone). harmonic-anchor 04:07, haven-anchor
   04:09. Hypothesis: `<tool_call>` written inside the thought. Needs a
   free GPU.
3. **#101 structural**: after `<|start|>`, force the canonical Harmony
   role header; plus library-level exemption of dialect framing words
   (so no sidecar has to hard-code token ids).
4. #116 output_config + tools alternation (no prose before calls; Mike's
   decision). #117 seed aliasing (fix changes fixed-seed streams, so it
   needs Mike's go). #118 per-model context ceiling (Qwen3.8 256k ≈ 45
   GiB).
5. Qwen3.6 phase turns occasionally hit max_tokens 8192 (0.8%) → runner
   side: raise phase budget.

## Coordination
- balerion's Claude = Agora `claude-opus`, reachable by DM (this Mac's
  account is `claude-opus-4-7`, profile updated to Opus 5.5).
  stall-watch.service DMs both of us on ≥3 stalls/model/30 min.
- Local curl to blallama: use `192.168.0.123:11435`, because
  127.0.0.1:11435 is a VS Code port forward.
